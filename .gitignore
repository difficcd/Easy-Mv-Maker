# Node / Vite
node_modules/
dist/
.vite/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS/editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Android / Gradle
android/.gradle/
android/**/build/
android/**/.cxx/
android/.idea/
android/local.properties
android/captures/

# Signing keys
*.jks
*.keystore


# Server runtime project storage
server/data/

# Claude Code local config
.claude/
.playwright-mcp/

# Dev-server output, written when the server is started with its log redirected.
*.log

# Screenshot written by the boot smoke test when it fails; CI keeps it as an artifact.
smoke-failure.png

# Scratch harness for comparing two builds visually. Not part of the project: it needs two
# builds served on two ports, and it is a technique rather than a check.
.visualcheck/
